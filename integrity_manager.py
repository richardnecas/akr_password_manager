import blockchain_manager
import hashlib
from typing import List
import password


#crc, hash, recount blockchainu

def check_blockchain(unchecked_list: List[password.Password]):
    return None


def encrypt_database(encoded_database: [{}], algorithm_type, number_of_bits):
    return None
